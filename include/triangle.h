#pragma once

#include <screen.h>
#include <vector.h>

void
draw_triangle_xy(screen_t* const screen,
                 const vec3_t a,
                 const vec3_t b,
                 const vec3_t c);
